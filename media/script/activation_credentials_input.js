jQuery(function(){
/*    var elements = {
        form_input_activation_credentials: jQuery('#form-activation-credentials')
    };

    var serial7_fields = jQuery('form input.serial7');
    var rules = {};
    jQuery.each(serial7_fields, function(index, field){        
        rules[field.name] = {
          required: true,
          maxlength: 7
        };
    });

    elements.form_input_activation_credentials.validate();
*/
});